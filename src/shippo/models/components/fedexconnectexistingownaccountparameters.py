"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import date
from decimal import Decimal
from enum import Enum
from pydantic import model_serializer
from pydantic.functional_serializers import PlainSerializer
from pydantic.functional_validators import BeforeValidator
from shippo.types import BaseModel, UNSET_SENTINEL
from shippo.utils import serialize_decimal, validate_decimal
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class VerificationOption(str, Enum):
    r"""Determines the verification option to use for the account registration (Enum: SMS, EMAIL, CALL, INVOICE)."""

    SMS = "SMS"
    EMAIL = "EMAIL"
    CALL = "CALL"
    INVOICE = "INVOICE"


class FedExConnectExistingOwnAccountParametersTypedDict(TypedDict):
    r"""In the case of masked fields, they should be handled carefully.

    Fields also must consider:
    - Not providing a *fields* in parameters will not result in a change to any configured value
    - Providing a value in a *masked field* with ****** (exactly 6 asterisks) will not change the configured value
    - Providing *field* with null will clear the configured value
    - Providing *field* with any other value will change the configured value and may affect the behavior of the account.

    """

    first_name: str
    r"""First name of the account holder"""
    last_name: str
    r"""Last name of the account holder"""
    phone_number: str
    r"""Phone number of the account holder"""
    from_address_st: str
    r"""Street address of the account holder"""
    from_address_city: str
    r"""City of the account holder"""
    from_address_state: str
    r"""State of the account holder"""
    from_address_zip: str
    r"""Zip code of the account holder"""
    from_address_country_iso2: str
    r"""Country of the account holder"""
    use_multi_factor_registration: bool
    r"""Must be true. FedEx accounts are registered using the multi-factor registration workflow."""
    verification_option: VerificationOption
    r"""Determines the verification option to use for the account registration (Enum: SMS, EMAIL, CALL, INVOICE)."""
    verification_pin: NotRequired[str]
    r"""(optional) The PIN to verify the account."""
    verification_invoice_number: NotRequired[str]
    r"""(optional) The invoice number to verify the account."""
    verification_invoice_amount: NotRequired[Decimal]
    r"""(optional) The invoice amount to verify the account."""
    verification_invoice_date: NotRequired[date]
    r"""(optional) The invoice date to verify the account. In the format `YYYY-MM-DD`."""
    verification_invoice_currency: NotRequired[str]
    r"""(optional) The invoice currency to verify the account."""


class FedExConnectExistingOwnAccountParameters(BaseModel):
    r"""In the case of masked fields, they should be handled carefully.

    Fields also must consider:
    - Not providing a *fields* in parameters will not result in a change to any configured value
    - Providing a value in a *masked field* with ****** (exactly 6 asterisks) will not change the configured value
    - Providing *field* with null will clear the configured value
    - Providing *field* with any other value will change the configured value and may affect the behavior of the account.

    """

    first_name: str
    r"""First name of the account holder"""

    last_name: str
    r"""Last name of the account holder"""

    phone_number: str
    r"""Phone number of the account holder"""

    from_address_st: str
    r"""Street address of the account holder"""

    from_address_city: str
    r"""City of the account holder"""

    from_address_state: str
    r"""State of the account holder"""

    from_address_zip: str
    r"""Zip code of the account holder"""

    from_address_country_iso2: str
    r"""Country of the account holder"""

    use_multi_factor_registration: bool
    r"""Must be true. FedEx accounts are registered using the multi-factor registration workflow."""

    verification_option: VerificationOption
    r"""Determines the verification option to use for the account registration (Enum: SMS, EMAIL, CALL, INVOICE)."""

    verification_pin: Optional[str] = None
    r"""(optional) The PIN to verify the account."""

    verification_invoice_number: Optional[str] = None
    r"""(optional) The invoice number to verify the account."""

    verification_invoice_amount: Annotated[
        Optional[Decimal],
        BeforeValidator(validate_decimal),
        PlainSerializer(serialize_decimal(True)),
    ] = None
    r"""(optional) The invoice amount to verify the account."""

    verification_invoice_date: Optional[date] = None
    r"""(optional) The invoice date to verify the account. In the format `YYYY-MM-DD`."""

    verification_invoice_currency: Optional[str] = None
    r"""(optional) The invoice currency to verify the account."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "verification_pin",
                "verification_invoice_number",
                "verification_invoice_amount",
                "verification_invoice_date",
                "verification_invoice_currency",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
