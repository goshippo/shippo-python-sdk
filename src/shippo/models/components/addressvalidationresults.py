"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .addressvalidationresultsmessage import (
    AddressValidationResultsMessage,
    AddressValidationResultsMessageTypedDict,
)
from pydantic import model_serializer
from shippo.types import BaseModel, UNSET_SENTINEL
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class AddressValidationResultsTypedDict(TypedDict):
    r"""Object that contains information regarding if an address had been validated or not. Also contains any messages
    generated during validation. Children keys are <code>is_valid</code>(boolean) and <code>messages</code>(array).
    """

    is_valid: NotRequired[bool]
    messages: NotRequired[List[AddressValidationResultsMessageTypedDict]]


class AddressValidationResults(BaseModel):
    r"""Object that contains information regarding if an address had been validated or not. Also contains any messages
    generated during validation. Children keys are <code>is_valid</code>(boolean) and <code>messages</code>(array).
    """

    is_valid: Optional[bool] = None

    messages: Optional[List[AddressValidationResultsMessage]] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["is_valid", "messages"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
